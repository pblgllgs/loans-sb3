server:
  port: 8090
spring:
  application:
    name: loans
  profiles:
    active: local
  config:
    import: optional:configserver:http://${CONFIG_SERVER_HOST:localhost}:8071/
  cloud:
    kubernetes:
      discovery:
        all-namespaces: true